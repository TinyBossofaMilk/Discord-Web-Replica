SERVER
    show SERVER you are active in, link to each one
    friends list

add admin privileges
    delete messages
    ban users
    invite users
    create mod roles
    create permissions for channel access for mod ruoles

USER
FRIENDS
    if sending friend req, shows if the friend does not exist
    if friend req already sent, do not send another req
    cannot send friend req if blocked


CHANNEL
    upvote functionality
    layout
    keep/show list of users

MESSAGES 
    delete messages
    edit messages (if you wrote it)
    reply to other messages(in chat)

DIRECT MESSAGES + GROUPS!!!
    DMschema?
        users involved
        messages[]
   
