extends layout 

block content
  form(method="POST" action="")
    div.form-group
      label(for="name") Name of Server
      input#name(name="name" placeholder="name" type="text")

    button(type="submit") Submit